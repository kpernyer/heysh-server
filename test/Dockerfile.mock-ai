FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    pydantic

# Copy mock service
COPY test/mock_ai_service.py /app/

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "mock_ai_service:app", "--host", "0.0.0.0", "--port", "8000"]
